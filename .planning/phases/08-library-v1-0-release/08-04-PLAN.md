---
phase: 08-library-v1-0-release
plan: 04
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
files_modified: []
autonomous: false
requirements:
  - REQ-803

must_haves:
  truths:
    - "All Phase 8 work is committed to main branch before tagging"
    - "Git tag v1.0.0 exists on main branch"
    - "Tag annotation message describes the release"
    - "Tag is pushed to origin remote"
    - "Human confirms tag is visible on GitHub"
  artifacts:
    - path: "git tag v1.0.0"
      provides: "Release marker on main branch"
      contains: "XjRibbon v1.0.0"
  key_links:
    - from: "local tag v1.0.0"
      to: "origin remote"
      via: "git push origin v1.0.0"
---

<objective>
Commit all Phase 8 changes to main, create the annotated v1.0.0 git tag, push it to origin, and verify the tag is live on GitHub.

Purpose: v1.0.0 is the distribution marker. Anyone cloning the repo can check out v1.0.0 to get the complete, tested, documented library. This is the "ship it" step.

Output: Annotated git tag v1.0.0 on main, pushed to origin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit all Phase 8 changes to main</name>
  <files></files>
  <action>
Stage and commit all Phase 8 changes that are not yet committed. The changes to commit are:
- README.md (rewritten GA-quality English README)
- README_TH.md (new Thai translation)
- desktop/XjRibbonGroup.xojo_code (doc comments added)
- desktop/XjRibbonItem.xojo_code (doc comments added)
- web/XjRibbonGroup.xojo_code (doc comments added)
- web/XjRibbonItem.xojo_code (doc comments added)
- Any .planning/phases/08-library-v1-0-release/ files

Run:
```bash
cd "/Users/worajedt/Xojo Projects/XjRibbon"
git status
```

Stage only the files above (do not use git add -A). Commit with message:
"docs(08): Phase 8 complete — GA README, Thai translation, doc comments"

If any of these files are already committed (from earlier plan executions), skip them — commit only what is unstaged.
  </action>
  <verify>
    <automated>cd "/Users/worajedt/Xojo Projects/XjRibbon" && git log --oneline -3</automated>
  </verify>
  <done>git log shows a commit containing "Phase 8" or "GA README" in the message. Working tree is clean (no uncommitted Phase 8 changes).</done>
</task>

<task type="auto">
  <name>Task 2: Create and push v1.0.0 annotated tag</name>
  <files></files>
  <action>
Create the annotated git tag and push it to origin:

```bash
cd "/Users/worajedt/Xojo Projects/XjRibbon"
git tag v1.0.0 -m "XjRibbon v1.0.0 — complete MS Office-style ribbon for Xojo Desktop and Web"
git push origin v1.0.0
```

Then verify the tag was created:
```bash
git tag -l "v1.0.0"
git show v1.0.0 --stat
```
  </action>
  <verify>
    <automated>cd "/Users/worajedt/Xojo Projects/XjRibbon" && git tag -l "v1.0.0"</automated>
  </verify>
  <done>`git tag -l "v1.0.0"` outputs "v1.0.0". `git show v1.0.0` shows the annotated tag message and the commit it points to.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Annotated git tag v1.0.0 created locally and pushed to origin. All Phase 8 changes committed to main.
  </what-built>
  <how-to-verify>
    1. Open https://github.com/YOUR_USERNAME/XjRibbon/tags (replace YOUR_USERNAME with the repo owner)
    2. Confirm v1.0.0 appears in the tags list
    3. Click v1.0.0 — confirm the annotation message reads "XjRibbon v1.0.0 — complete MS Office-style ribbon for Xojo Desktop and Web"
    4. Optionally: check that README.md on the main branch shows the new GA-quality content
  </how-to-verify>
  <resume-signal>Type "approved" when the tag is visible on GitHub, or describe any issue (e.g., "push failed — need auth")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git → origin remote | Pushing tag to GitHub; requires authenticated git remote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | git tag | accept | Tag is annotated (not lightweight) — annotation message provides release provenance; SHA is immutable |
| T-08-05 | Repudiation | git push | mitigate | Commit and tag are signed by git user config (worajedt.s@psp-asia.com); push requires authenticated remote |
</threat_model>

<verification>
- `git tag -l "v1.0.0"` returns "v1.0.0"
- `git show v1.0.0` shows annotation message containing "XjRibbon v1.0.0"
- GitHub tags page shows v1.0.0
- Working tree is clean after all commits
</verification>

<success_criteria>
Annotated tag v1.0.0 is live on GitHub. Anyone can run `git clone` and `git checkout v1.0.0` to receive the complete, documented XjRibbon v1.0.0 library. Phase 8 and the v1.0 milestone are complete.
</success_criteria>

<output>
After completion, create `.planning/phases/08-library-v1-0-release/08-04-SUMMARY.md`
</output>
