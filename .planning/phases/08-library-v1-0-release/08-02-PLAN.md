---
phase: 08-library-v1-0-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - desktop/XjRibbonGroup.xojo_code
  - desktop/XjRibbonItem.xojo_code
  - web/XjRibbonGroup.xojo_code
  - web/XjRibbonItem.xojo_code
autonomous: true
requirements:
  - REQ-802

must_haves:
  truths:
    - "Every public factory method in XjRibbonGroup has a single-line doc comment above its #tag Method line"
    - "Every public property in XjRibbonItem has a single-line doc comment above its #tag Property line"
    - "AddMenuItem in XjRibbonItem has a doc comment"
    - "Doc comments are in Xojo single-line style: // comment"
    - "Both desktop/ and web/ files are updated identically"
    - "No existing code logic is changed — only comment lines are added"
  artifacts:
    - path: "desktop/XjRibbonGroup.xojo_code"
      provides: "Documented desktop group factory methods"
      contains: "// Adds a large button"
    - path: "desktop/XjRibbonItem.xojo_code"
      provides: "Documented desktop item properties"
      contains: "// Picture displayed"
    - path: "web/XjRibbonGroup.xojo_code"
      provides: "Documented web group factory methods"
      contains: "// Adds a large button"
    - path: "web/XjRibbonItem.xojo_code"
      provides: "Documented web item properties"
      contains: "// Picture displayed"
  key_links:
    - from: "doc comments"
      to: "method/property definitions below"
      via: "adjacent line placement"
---

<objective>
Add Xojo-style single-line doc comments to all public methods and key properties in the four library files: desktop/XjRibbonGroup.xojo_code, desktop/XjRibbonItem.xojo_code, web/XjRibbonGroup.xojo_code, web/XjRibbonItem.xojo_code.

Purpose: Developers using these classes in the Xojo IDE will see inline documentation when reading the source. This is the standard documentation layer for Xojo library code.

Output: Four modified .xojo_code files with // comment lines inserted above each public method and property definition. No logic changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- Current file structure — doc comments go INSIDE each #tag Method or #tag Property block, -->
<!-- as the first line of the block body (after the Function/Sub/Property declaration line). -->
<!-- In Xojo source format, the comment is the first statement inside the method body,       -->
<!-- or for properties: insert a // comment line immediately BEFORE the #tag Property line.  -->

<!-- IMPORTANT: Xojo .xojo_code format places code INSIDE the #tag block.                   -->
<!-- For methods: the comment goes as the first line of the method body.                     -->
<!-- For properties: the comment is placed on the line immediately before #tag Property.     -->

desktop/XjRibbonGroup.xojo_code current structure (7 public methods):
  AddItem(item)                   — internal helper, skip
  AddLargeButton(caption, tag)    — public
  AddLargeButton(caption, tag, icon) — public (overload)
  AddSmallButton(caption, tag)    — public
  AddDropdownButton(caption, tag) — public
  AddSplitButton(caption, tag)    — public
  AddCheckBox(caption, tag, initialState) — public
  AddSeparator()                  — public Sub

desktop/XjRibbonItem.xojo_code current structure (public properties and method):
  AddMenuItem(caption, tag)       — public method
  Caption As String               — public property (skip — self-documenting name)
  Tag As String                   — public property (skip — self-documenting name)
  IsEnabled As Boolean            — public property
  ItemType As Integer             — public property
  Icon As Picture                 — public property
  TooltipText As String           — public property
  IsToggle As Boolean             — public property
  IsToggleActive As Boolean       — public property
  IsSplitButton As Boolean        — public property
  KeyTip As String                — public property
  mMenuItems() As DesktopMenuItem — private, skip
  mBoundsX/Y/W/H                  — private, skip
  mIsHovered, mIsPressed          — private, skip

web/ files: identical structure except AddMenuItem uses WebMenuItem instead of DesktopMenuItem.
Web XjRibbonItem also lacks KeyTip property (web skips keyboard nav).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add doc comments to desktop/XjRibbonGroup.xojo_code</name>
  <files>desktop/XjRibbonGroup.xojo_code</files>
  <action>
Rewrite desktop/XjRibbonGroup.xojo_code to add a single // comment line as the first statement inside each public factory method body. Do NOT add comments to AddItem (internal). Do NOT change any existing code lines.

Comment text to use for each method (insert as first line inside the method body, after the Function/Sub declaration):

AddLargeButton(caption, tag):
  // Adds a large button (32px icon + label below) to this group. Returns the new XjRibbonItem.

AddLargeButton(caption, tag, icon) overload:
  // Adds a large button with an explicit icon Picture. Returns the new XjRibbonItem.

AddSmallButton(caption, tag):
  // Adds a small button (16px icon + label right, stacks 3-per-column). Returns the new XjRibbonItem.

AddDropdownButton(caption, tag):
  // Adds a dropdown button — the entire button area opens a popup menu. Returns the new XjRibbonItem.

AddSplitButton(caption, tag):
  // Adds a split button — body fires ItemPressed directly; arrow area opens the popup menu. Returns the new XjRibbonItem.

AddCheckBox(caption, tag, initialState):
  // Adds a checkbox item (☐/☑ glyph + label, no icon slot). initialState sets the default checked state. Returns the new XjRibbonItem.

AddSeparator():
  // Adds a visual separator — a zero-width column boundary with no interaction.

The final file must compile identically to the original (same logic, only comments added).
  </action>
  <verify>
    <automated>grep -c "// Adds a" "/Users/worajedt/Xojo Projects/XjRibbon/desktop/XjRibbonGroup.xojo_code"</automated>
  </verify>
  <done>desktop/XjRibbonGroup.xojo_code contains 7 doc comment lines (one per public factory method including both AddLargeButton overloads). All original code lines unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add doc comments to desktop/XjRibbonItem.xojo_code, web/XjRibbonGroup.xojo_code, and web/XjRibbonItem.xojo_code</name>
  <files>desktop/XjRibbonItem.xojo_code, web/XjRibbonGroup.xojo_code, web/XjRibbonItem.xojo_code</files>
  <action>
Update the three remaining files with doc comments using the same approach as Task 1.

**desktop/XjRibbonItem.xojo_code** — add comment as first line inside each method/property block:

AddMenuItem method body (first line):
  // Adds a menu item to this button's popup menu. Use with Dropdown and SplitButton types.

For properties, place the // comment on the line immediately BEFORE the #tag Property line:

Before `#tag Property` for IsEnabled:
  // Controls whether this item responds to mouse events and renders as active.

Before `#tag Property` for ItemType:
  // Internal item type: 0=Large, 1=Small, 2=Dropdown/Split, 3=CheckBox, 4=Separator.

Before `#tag Property` for Icon:
  // Picture displayed as the button icon. Large buttons: 32x32px. Small buttons: 16x16px.

Before `#tag Property` for TooltipText:
  // Tooltip shown on hover. Leave empty for no tooltip.

Before `#tag Property` for IsToggle:
  // When True, this item behaves as a toggle button — maintaining pressed/depressed state.

Before `#tag Property` for IsToggleActive:
  // Current toggle state. True = active/pressed. Read/write. Also used as CheckBox checked state.

Before `#tag Property` for IsSplitButton:
  // When True on a Dropdown button, the body fires ItemPressed directly; only the arrow opens the menu.

Before `#tag Property` for KeyTip:
  // Single character shown as a badge when Alt/F6 activates KeyTip mode (Desktop only).

Do NOT add comments before Caption, Tag, or any private (m-prefixed) property.

---

**web/XjRibbonGroup.xojo_code** — identical comments to desktop/XjRibbonGroup.xojo_code. Apply the same 7 comments to the same 7 methods. AddItem gets no comment.

---

**web/XjRibbonItem.xojo_code** — identical comments to desktop/XjRibbonItem.xojo_code EXCEPT:
- Do NOT add a comment for KeyTip (web XjRibbonItem has no KeyTip property)
- AddMenuItem comment uses WebMenuItem internally but the comment text is the same

No logic changes to any of the three files.
  </action>
  <verify>
    <automated>grep -c "// Adds a menu item" "/Users/worajedt/Xojo Projects/XjRibbon/desktop/XjRibbonItem.xojo_code" && grep -c "// Adds a" "/Users/worajedt/Xojo Projects/XjRibbon/web/XjRibbonGroup.xojo_code" && grep -c "// Current toggle state" "/Users/worajedt/Xojo Projects/XjRibbon/web/XjRibbonItem.xojo_code"</automated>
  </verify>
  <done>All three files contain their respective doc comments. desktop/XjRibbonItem.xojo_code has AddMenuItem comment + 8 property comments. web/XjRibbonGroup.xojo_code has 7 method comments. web/XjRibbonItem.xojo_code has AddMenuItem comment + 7 property comments (no KeyTip). No logic changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source file edits | Internal .xojo_code modifications — no external input, no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Tampering | .xojo_code files | accept | Comments are read-only documentation; any logic change would be caught by Xojo IDE compilation |
</threat_model>

<verification>
- desktop/XjRibbonGroup.xojo_code: 7 doc comments present (one per public factory method)
- desktop/XjRibbonItem.xojo_code: AddMenuItem doc comment + doc comments on IsEnabled, ItemType, Icon, TooltipText, IsToggle, IsToggleActive, IsSplitButton, KeyTip
- web/XjRibbonGroup.xojo_code: same 7 doc comments as desktop equivalent
- web/XjRibbonItem.xojo_code: same comments as desktop equivalent minus KeyTip
- No m-prefixed (private) properties have comments
- No code logic lines altered in any file
</verification>

<success_criteria>
All four library files contain Xojo-style // doc comments above every public method and key property. A developer reading the source in the Xojo IDE will immediately understand the purpose of each factory method and item property without consulting external documentation.
</success_criteria>

<output>
After completion, create `.planning/phases/08-library-v1-0-release/08-02-SUMMARY.md`
</output>
