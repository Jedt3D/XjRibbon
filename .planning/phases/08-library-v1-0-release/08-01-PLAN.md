---
phase: 08-library-v1-0-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - REQ-801
  - REQ-804

must_haves:
  truths:
    - "README.md exists at repository root with full GA-quality content"
    - "All 7 control types are documented with method signatures and descriptions"
    - "Both Desktop and Web Quick Start code examples are present"
    - "API Reference covers XjRibbon, XjRibbonTab, XjRibbonGroup, XjRibbonItem"
    - "Designer section describes XjRibbon Designer v2.0.0"
    - "Screenshot placeholders use [screenshot](description) format throughout"
    - "kXjRibbonVersion constant is listed in API Reference"
  artifacts:
    - path: "README.md"
      provides: "Full GA-quality English README"
      contains: "## API Reference"
  key_links:
    - from: "README.md Quick Start"
      to: "XjRibbonGroup.AddLargeButton / AddSmallButton"
      via: "code example"
    - from: "README.md Control Types table"
      to: "All 7 AddXxx factory methods"
      via: "Method column"
---

<objective>
Replace the existing README.md with a full GA-quality English README for XjRibbon v1.0.

Purpose: This is the primary distribution artifact — the first thing a developer sees when evaluating XjRibbon. It must read as a polished, shippable product with complete API coverage, working code examples, and clear structure.

Output: README.md at repository root — screenshot placeholders, Quick Start for Desktop and Web, full Control Types table, complete API Reference, Advanced Topics, Web Notes, Designer section, MIT license footer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Public API extracted from desktop/XjRibbon.xojo_code and desktop/XjRibbonGroup.xojo_code -->
<!-- These are the exact method signatures to document -->

From desktop/XjRibbon.xojo_code (public methods and events):
```xojo
Function AddTab(caption As String) As XjRibbonTab
Function AddContextualTab(caption As String, contextGroup As String, accentColor As Color) As XjRibbonTab
Sub SelectTab(index As Integer)
Sub Clear()
Sub ShowContextualTabs(contextGroup As String)
Sub HideContextualTabs(contextGroup As String)
Sub SetCollapsed(value As Boolean)
Function IsCollapsed() As Boolean
Function GetToggleState(tag As String) As Boolean
Sub SetToggleState(tag As String, value As Boolean)

Event ItemPressed(tag As String)
Event DropdownMenuAction(itemTag As String, menuItemTag As String)
Event CollapseStateChanged(isCollapsed As Boolean)

Constant kXjRibbonVersion As String = "1.0.0"
```

From desktop/XjRibbonGroup.xojo_code (all public factory methods):
```xojo
Function AddLargeButton(caption As String, tag As String) As XjRibbonItem
Function AddLargeButton(caption As String, tag As String, icon As Picture) As XjRibbonItem
Function AddSmallButton(caption As String, tag As String) As XjRibbonItem
Function AddDropdownButton(caption As String, tag As String) As XjRibbonItem
Function AddSplitButton(caption As String, tag As String) As XjRibbonItem
Function AddCheckBox(caption As String, tag As String, initialChecked As Boolean = False) As XjRibbonItem
Sub AddSeparator()
```

From desktop/XjRibbonItem.xojo_code (public properties):
```xojo
Property Caption As String
Property Tag As String
Property Icon As Picture
Property TooltipText As String
Property IsEnabled As Boolean = True
Property ItemType As Integer = 0
Property IsToggle As Boolean = False
Property IsToggleActive As Boolean = False
Property IsSplitButton As Boolean = False
Property KeyTip As String
Sub AddMenuItem(caption As String, tag As String)
```

From desktop/XjRibbonTab.xojo_code (key property):
```xojo
Property KeyTip As String
Function AddNewGroup(caption As String) As XjRibbonGroup
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write full GA-quality README.md</name>
  <files>README.md</files>
  <action>
Overwrite the existing README.md with the complete content specified below. Write it verbatim — do not shorten, summarize, or paraphrase any section. Every heading, table row, code block, and screenshot placeholder must appear exactly as specified.

The file content to write:

---

# XjRibbon

[screenshot](full ribbon with all 7 control types visible — Home tab showing Large/Small/Toggle buttons, View tab showing SplitButton + CheckBox items + Separator)

> MS Office-style ribbon toolbar for Xojo Desktop and Web.

## Features

- 7 control types: Large Button, Small Button, Toggle Button, Dropdown, SplitButton, CheckBox, Separator
- Desktop (DesktopCanvas) and Web (WebCanvas) — identical API
- Dark mode, HiDPI, keyboard navigation (KeyTips) on Desktop
- Visual designer with code generator

## Requirements

- Xojo 2024r3 or later
- macOS, Windows, or Linux (Desktop); any web browser (Web)

## Quick Start (Desktop)

[screenshot](ribbon running in the demo app — Home tab active)

```xojo
// 1. Add XjRibbon canvas to your window (Height = 122)
// 2. In Opening event:

Var homeTab As XjRibbonTab = XjRibbon1.AddTab("Home")
homeTab.KeyTip = "H"

Var clipGroup As XjRibbonGroup = homeTab.AddNewGroup("Clipboard")
Call clipGroup.AddLargeButton("Paste", "clipboard.paste")
Call clipGroup.AddSmallButton("Cut", "clipboard.cut")
Call clipGroup.AddSmallButton("Copy", "clipboard.copy")
```

Handle events in XjRibbon1:
```xojo
Sub ItemPressed(tag As String)
  MessageBox "Pressed: " + tag
End Sub

Sub DropdownMenuAction(itemTag As String, menuItemTag As String)
  MessageBox itemTag + " → " + menuItemTag
End Sub
```

## Quick Start (Web)

[screenshot](web ribbon in browser — same layout as desktop)

Same API — add XjRibbon to your WebPage instead of a DesktopWindow.

## Control Types

[screenshot](all 7 control types labeled — View tab with SplitButton, CheckBox rows, Separator gap visible)

| Type | Method | Description |
|------|--------|-------------|
| Large Button | `group.AddLargeButton(caption, tag)` | 32px icon + label below, full group height |
| Small Button | `group.AddSmallButton(caption, tag)` | 16px icon + label right, stacks 3-per-column |
| Toggle Button | `group.AddLargeButton(caption, tag)` + `.IsToggle = True` | Pressed/active state |
| Dropdown Button | `group.AddDropdownButton(caption, tag)` + `.AddMenuItem(...)` | Opens popup menu |
| SplitButton | `group.AddSplitButton(caption, tag)` + `.AddMenuItem(...)` | Body fires ItemPressed; arrow opens menu |
| CheckBox | `group.AddCheckBox(caption, tag, initialChecked)` | ☐/☑ glyph with label |
| Separator | `group.AddSeparator()` | Visual gap between control clusters |

## API Reference

### XjRibbon (Canvas)

#### Tabs
```xojo
Function AddTab(caption As String) As XjRibbonTab
Function AddContextualTab(caption As String, contextGroup As String, accentColor As Color) As XjRibbonTab
Sub ShowContextualTabs(contextGroup As String)
Sub HideContextualTabs(contextGroup As String)
Sub SelectTab(index As Integer)
```

#### Items
```xojo
Function GetToggleState(tag As String) As Boolean
Sub SetToggleState(tag As String, value As Boolean)
Sub SetCollapsed(value As Boolean)
Function IsCollapsed() As Boolean
Sub Clear()
```

#### Events
```xojo
Event ItemPressed(tag As String)
Event DropdownMenuAction(itemTag As String, menuItemTag As String)
Event CollapseStateChanged(isCollapsed As Boolean)
```

#### Constants
```xojo
kXjRibbonVersion As String  // "1.0.0"
```

### XjRibbonTab
```xojo
Function AddNewGroup(caption As String) As XjRibbonGroup
Property KeyTip As String
```

### XjRibbonGroup
```xojo
Function AddLargeButton(caption As String, tag As String) As XjRibbonItem
Function AddLargeButton(caption As String, tag As String, icon As Picture) As XjRibbonItem
Function AddSmallButton(caption As String, tag As String) As XjRibbonItem
Function AddDropdownButton(caption As String, tag As String) As XjRibbonItem
Function AddSplitButton(caption As String, tag As String) As XjRibbonItem
Function AddCheckBox(caption As String, tag As String, initialChecked As Boolean = False) As XjRibbonItem
Sub AddSeparator()
```

### XjRibbonItem
```xojo
Property Icon As Picture
Property TooltipText As String
Property IsEnabled As Boolean
Property IsToggle As Boolean
Property IsToggleActive As Boolean
Property IsSplitButton As Boolean
Property KeyTip As String
Sub AddMenuItem(caption As String, tag As String)
```

## Advanced Topics

### Multi-line Button Labels
Use Chr(10) to split a label across two lines:
```xojo
Var navPane As XjRibbonItem = group.AddSplitButton("Navigation" + Chr(10) + "pane", "nav")
```

### Icons
```xojo
Var btn As XjRibbonItem = group.AddLargeButton("Save", "file.save")
btn.Icon = SaveIcon  // 32×32 Picture
```

### Contextual Tabs
[screenshot](contextual tab with accent color visible — Table Tools style)
```xojo
Var tableTab As XjRibbonTab = XjRibbon1.AddContextualTab("Table Tools", "table", Color.RGB(68, 114, 196))
// Show when table is selected:
XjRibbon1.ShowContextualTabs("table")
```

### KeyTips (Desktop only)
```xojo
homeTab.KeyTip = "H"
pasteItem.KeyTip = "V"
// Press Alt to reveal badges; type key to activate
```

### Dark Mode
XjRibbon detects the system dark mode automatically. No code required.

## Web Notes

The Web library shares the same API with these constraints:
- No KeyTip keyboard navigation (web is mouse-driven)
- Hover via JS mousemove injection (automatic — no setup needed)
- Apply 120% scaling in your layout (canvas height = 146 instead of 122)

## Designer

[screenshot](XjRibbon Designer v2.0.0 — showing tree structure with all 7 control types, inspector panel, and Copy Code button)

**XjRibbon Designer v2.0.0** — visual ribbon builder with code generator.

Supports all 7 control types. Build your ribbon visually, then click **Copy Code** to paste Xojo code directly into your project's Opening/Shown event.

- Save/load `.ribbon` project files
- Dark mode aware
- Generates ready-to-run Xojo code for Desktop or Web

## License
MIT — see [LICENSE](LICENSE)

---

Write this content exactly as shown above. Replace the existing README.md completely.
  </action>
  <verify>
    <automated>grep -c "## API Reference" "/Users/worajedt/Xojo Projects/XjRibbon/README.md" && grep -c "AddSplitButton\|AddCheckBox\|AddSeparator" "/Users/worajedt/Xojo Projects/XjRibbon/README.md" && grep -c "\[screenshot\]" "/Users/worajedt/Xojo Projects/XjRibbon/README.md"</automated>
  </verify>
  <done>README.md contains "## API Reference" section, all three new factory methods (AddSplitButton, AddCheckBox, AddSeparator), and at least 4 screenshot placeholders in [screenshot](description) format.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README content → public repo | Documentation artifact — read-only, no executable code, no trust boundary concerns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | README.md | accept | No secrets or credentials referenced; code examples use placeholder tags only |
</threat_model>

<verification>
- README.md exists at repository root
- File contains all 7 control types in the Control Types table
- All AddXxx method signatures match those in desktop/XjRibbonGroup.xojo_code
- Screenshot placeholders use `[screenshot](description)` format (not broken img tags)
- kXjRibbonVersion constant listed as "1.0.0"
- Web Notes section present with 120% scaling note
- Designer section mentions v2.0.0
</verification>

<success_criteria>
README.md at repository root contains: Features list, Requirements, Quick Start (Desktop + Web), Control Types table with all 7 types, complete API Reference for all four classes, Advanced Topics (multi-line labels, icons, contextual tabs, KeyTips, dark mode), Web Notes, Designer section, MIT License footer — with screenshot placeholders in place of actual images.
</success_criteria>

<output>
After completion, create `.planning/phases/08-library-v1-0-release/08-01-SUMMARY.md`
</output>
