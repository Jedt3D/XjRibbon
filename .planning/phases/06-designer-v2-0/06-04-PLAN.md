---
phase: 06-designer-v2-0
plan: 04
type: execute
wave: 3
depends_on:
  - "06-02"
  - "06-03"
files_modified:
  - designer/MainWindow.xojo_window
autonomous: true
requirements:
  - REQ-607

must_haves:
  truths:
    - "GenerateCode produces .AddSplitButton(...) call for splitbutton rows"
    - "GenerateCode produces .AddLargeButton(...) + .IsToggle = True (+ optional .IsToggleActive = True) for toggle rows"
    - "GenerateCode produces .AddCheckBox(...) or Call .AddCheckBox(...) for checkbox rows"
    - "The generated code is valid Xojo — menu items appended for splitbutton same as dropdown"
  artifacts:
    - path: "designer/MainWindow.xojo_window"
      provides: "GenerateCode Select Case with splitbutton, toggle, checkbox cases"
      contains: "AddSplitButton"
  key_links:
    - from: "GenerateCode inner item loop"
      to: "grpVar.AddSplitButton / AddLargeButton+IsToggle / AddCheckBox"
      via: "Select Case it with new cases"
      pattern: "Case \"splitbutton\"\|Case \"toggle\"\|Case \"checkbox\""
---

<objective>
Restructure the GenerateCode item-dispatch block from a simple `If it = "large" Or it = "small"` into a `Select Case it` with branches for all five item types. Add Case blocks for "splitbutton", "toggle", and "checkbox" generating valid Xojo API calls.

Purpose: Without code gen support the designer produces no output for new types — the core user value of the tool is broken for Phase 5 item types.
Output: "Copy Toolbar Code" generates correct Xojo code for all five item types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-designer-v2-0/06-RESEARCH.md
@.planning/phases/06-designer-v2-0/06-02-SUMMARY.md
@.planning/phases/06-designer-v2-0/06-03-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns from GenerateCode (lines 1102-1201) -->

Current item dispatch (lines 1147-1188):
```xojo
          Var it As String = id.Value("type")
          If it = "tab" Or it = "group" Then Exit
          
          If it = "large" Or it = "small" Then
            Var cap As String = id.Value("caption")
            Var tag As String = id.Lookup("tag", "")
            Var tip As String = id.Lookup("tooltipText", "")
            Var enabled As Boolean = id.Lookup("isEnabled", True)
            Var miList() As Dictionary = id.Lookup("menuItems", Nil)
            Var hasMI As Boolean = (miList <> Nil And miList.Count > 0)
            Var isDropdown As Boolean = (it = "large" And hasMI)
            Var hasExtras As Boolean = (tip <> "" Or Not enabled Or isDropdown)
            Var itemVar As String = UniqueVarName(SanitizeVarName(cap) + "Item", usedNames)
            
            If isDropdown Then
              code = code + "Var " + itemVar + " As XjRibbonItem = " + grpVar + ".AddDropdownButton(""" + cap + """, """ + tag + """)" + eol
            ElseIf hasExtras Then
              If it = "large" Then
                code = code + "Var " + itemVar + " As XjRibbonItem = " + grpVar + ".AddLargeButton(""" + cap + """, """ + tag + """)" + eol
              Else
                code = code + "Var " + itemVar + " As XjRibbonItem = " + grpVar + ".AddSmallButton(""" + cap + """, """ + tag + """)" + eol
              End If
            Else
              If it = "large" Then
                code = code + "Call " + grpVar + ".AddLargeButton(""" + cap + """, """ + tag + """)" + eol
              Else
                code = code + "Call " + grpVar + ".AddSmallButton(""" + cap + """, """ + tag + """)" + eol
              End If
            End If
            
            If tip <> "" Then
              code = code + itemVar + ".TooltipText = """ + tip + """" + eol
            End If
            If Not enabled Then
              code = code + itemVar + ".IsEnabled = False" + eol
            End If
            If isDropdown Then
              For Each mi As Dictionary In miList
                code = code + itemVar + ".AddMenuItem(""" + mi.Value("caption").StringValue + """, """ + mi.Value("tag").StringValue + """)" + eol
              Next
            End If
          End If
```

UniqueVarName(baseName, usedNames) — allocates unique variable names
SanitizeVarName(caption) — converts caption to camelCase identifier
eol variable already declared at top of GenerateCode
grpVar already declared in outer loop
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restructure GenerateCode item dispatch as Select Case with new type branches</name>
  <files>designer/MainWindow.xojo_window</files>
  <read_first>designer/MainWindow.xojo_window (lines 1140-1200 for the item dispatch block inside the innermost While loop)</read_first>
  <action>
CLOSE Xojo IDE before editing this file.

Replace the entire item dispatch block (from `Var it As String = id.Value("type")` through the closing `End If` of the `If it = "large" Or it = "small" Then` block) with the following Select Case structure:

```xojo
          Var it As String = id.Value("type")
          If it = "tab" Or it = "group" Then Exit
          
          Select Case it
          Case "large", "small"
            Var cap As String = id.Value("caption")
            Var tag As String = id.Lookup("tag", "")
            Var tip As String = id.Lookup("tooltipText", "")
            Var enabled As Boolean = id.Lookup("isEnabled", True)
            Var miList() As Dictionary = id.Lookup("menuItems", Nil)
            Var hasMI As Boolean = (miList <> Nil And miList.Count > 0)
            Var isDropdown As Boolean = (it = "large" And hasMI)
            Var hasExtras As Boolean = (tip <> "" Or Not enabled Or isDropdown)
            Var itemVar As String = UniqueVarName(SanitizeVarName(cap) + "Item", usedNames)
            
            If isDropdown Then
              code = code + "Var " + itemVar + " As XjRibbonItem = " + grpVar + ".AddDropdownButton(""" + cap + """, """ + tag + """)" + eol
            ElseIf hasExtras Then
              If it = "large" Then
                code = code + "Var " + itemVar + " As XjRibbonItem = " + grpVar + ".AddLargeButton(""" + cap + """, """ + tag + """)" + eol
              Else
                code = code + "Var " + itemVar + " As XjRibbonItem = " + grpVar + ".AddSmallButton(""" + cap + """, """ + tag + """)" + eol
              End If
            Else
              If it = "large" Then
                code = code + "Call " + grpVar + ".AddLargeButton(""" + cap + """, """ + tag + """)" + eol
              Else
                code = code + "Call " + grpVar + ".AddSmallButton(""" + cap + """, """ + tag + """)" + eol
              End If
            End If
            
            If tip <> "" Then
              code = code + itemVar + ".TooltipText = """ + tip + """" + eol
            End If
            If Not enabled Then
              code = code + itemVar + ".IsEnabled = False" + eol
            End If
            If isDropdown Then
              For Each mi As Dictionary In miList
                code = code + itemVar + ".AddMenuItem(""" + mi.Value("caption").StringValue + """, """ + mi.Value("tag").StringValue + """)" + eol
              Next
            End If
            
          Case "splitbutton"
            Var cap As String = id.Value("caption")
            Var tag As String = id.Lookup("tag", "")
            Var tip As String = id.Lookup("tooltipText", "")
            Var enabled As Boolean = id.Lookup("isEnabled", True)
            Var miList() As Dictionary = id.Lookup("menuItems", Nil)
            Var itemVar As String = UniqueVarName(SanitizeVarName(cap) + "Item", usedNames)
            code = code + "Var " + itemVar + " As XjRibbonItem = " + grpVar + ".AddSplitButton(""" + cap + """, """ + tag + """)" + eol
            If tip <> "" Then
              code = code + itemVar + ".TooltipText = """ + tip + """" + eol
            End If
            If Not enabled Then
              code = code + itemVar + ".IsEnabled = False" + eol
            End If
            If miList <> Nil Then
              For Each mi As Dictionary In miList
                code = code + itemVar + ".AddMenuItem(""" + mi.Value("caption").StringValue + """, """ + mi.Value("tag").StringValue + """)" + eol
              Next
            End If
            
          Case "toggle"
            Var cap As String = id.Value("caption")
            Var tag As String = id.Lookup("tag", "")
            Var tip As String = id.Lookup("tooltipText", "")
            Var enabled As Boolean = id.Lookup("isEnabled", True)
            Var isActive As Boolean = id.Lookup("isToggleActive", False)
            Var itemVar As String = UniqueVarName(SanitizeVarName(cap) + "Item", usedNames)
            code = code + "Var " + itemVar + " As XjRibbonItem = " + grpVar + ".AddLargeButton(""" + cap + """, """ + tag + """)" + eol
            code = code + itemVar + ".IsToggle = True" + eol
            If isActive Then
              code = code + itemVar + ".IsToggleActive = True" + eol
            End If
            If tip <> "" Then
              code = code + itemVar + ".TooltipText = """ + tip + """" + eol
            End If
            If Not enabled Then
              code = code + itemVar + ".IsEnabled = False" + eol
            End If
            
          Case "checkbox"
            Var cap As String = id.Value("caption")
            Var tag As String = id.Lookup("tag", "")
            Var isActive As Boolean = id.Lookup("isToggleActive", False)
            If isActive Then
              Var itemVar As String = UniqueVarName(SanitizeVarName(cap) + "Item", usedNames)
              code = code + "Var " + itemVar + " As XjRibbonItem = " + grpVar + ".AddCheckBox(""" + cap + """, """ + tag + """, True)" + eol
            Else
              code = code + "Call " + grpVar + ".AddCheckBox(""" + cap + """, """ + tag + """)" + eol
            End If
            
          End Select
```

Important: The `If it = "tab" Or it = "group" Then Exit` guard line stays BEFORE the Select Case (unchanged). The Select Case replaces only the `If it = "large" Or it = "small" Then ... End If` block.
  </action>
  <verify>
    <automated>grep -n "Case \"splitbutton\"\|Case \"toggle\"\|Case \"checkbox\"\|AddSplitButton\|IsToggle = True\|AddCheckBox" "/Users/worajedt/Xojo Projects/XjRibbon/designer/MainWindow.xojo_window" | grep -v "#tag\|--"</automated>
  </verify>
  <acceptance_criteria>
- `grep -n 'Case "splitbutton"' designer/MainWindow.xojo_window` returns at least 2 matches (SelectionChanged + GenerateCode)
- `grep -n 'Case "toggle"' designer/MainWindow.xojo_window` returns at least 2 matches (SelectionChanged/PopulateInspector + GenerateCode)
- `grep -n 'Case "checkbox"' designer/MainWindow.xojo_window` returns at least 2 matches
- `grep -n 'AddSplitButton' designer/MainWindow.xojo_window` returns 1 match inside GenerateCode
- `grep -n '\.IsToggle = True' designer/MainWindow.xojo_window` returns 1 match inside GenerateCode
- `grep -n 'IsToggleActive = True' designer/MainWindow.xojo_window` returns 1 match inside GenerateCode (inside toggle Case)
- `grep -n 'AddCheckBox' designer/MainWindow.xojo_window` returns 2 matches (the Call form and the Var form inside checkbox Case)
- `grep -n 'Case "large", "small"' designer/MainWindow.xojo_window` returns 1 match (confirms restructure to Select Case)
- The old `If it = "large" Or it = "small" Then` line no longer exists as a standalone If (only the Select Case form remains)
  </acceptance_criteria>
  <done>GenerateCode produces correct Xojo API calls for splitbutton (AddSplitButton + menu items), toggle (AddLargeButton + .IsToggle = True + optional .IsToggleActive = True), and checkbox (Call .AddCheckBox or Var .AddCheckBox(..., True)).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GenerateCode → Clipboard | Generated code string written to clipboard; no injection vector since this is Xojo source, not executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | GenerateCode string builder | accept | Code is generated from in-app Dictionary data only; no user-controlled string is injected into Xojo code structure (caption/tag are string literals in generated output, sanitized by wrapping in triple-quotes) |
</threat_model>

<verification>
After task completes:
1. Add a SplitButton row with 2 menu items → Copy Toolbar Code → paste should contain `grpVar.AddSplitButton(...)` and two `.AddMenuItem(...)` lines
2. Add a Toggle Button row with "Default Active?" checked → generated code has `.IsToggle = True` and `.IsToggleActive = True`
3. Add a Toggle Button row with "Default Active?" unchecked → generated code has `.IsToggle = True` but NO `.IsToggleActive` line
4. Add a CheckBox row with "Default Active?" unchecked → generated code has `Call grpVar.AddCheckBox(...)`
5. Add a CheckBox row with "Default Active?" checked → generated code has `Var xxxItem As XjRibbonItem = grpVar.AddCheckBox(..., True)`
6. Existing Large/Small/Dropdown generation still works correctly (no regression)
</verification>

<success_criteria>
- GenerateCode item dispatch uses Select Case structure
- Case "splitbutton" generates AddSplitButton + menu items loop
- Case "toggle" generates AddLargeButton + .IsToggle = True + conditional .IsToggleActive = True
- Case "checkbox" generates conditional Call/.Var form of AddCheckBox
- Case "large", "small" behavior is identical to before (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/06-designer-v2-0/06-04-SUMMARY.md`
</output>
