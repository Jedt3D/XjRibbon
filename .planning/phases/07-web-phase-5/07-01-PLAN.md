---
phase: "07-web-phase-5"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/XjRibbonItem.xojo_code
autonomous: true
requirements:
  - REQ-701
must_haves:
  truths:
    - "web/XjRibbonItem has an IsSplitButton property defaulting to False"
    - "Web XjRibbonGroup.AddSplitButton can set IsSplitButton = True on a newly created item without compile error"
  artifacts:
    - path: "web/XjRibbonItem.xojo_code"
      provides: "IsSplitButton property storage for SplitButton mode"
      contains: "IsSplitButton As Boolean = False"
  key_links:
    - from: "web/XjRibbonGroup.AddSplitButton"
      to: "XjRibbonItem.IsSplitButton"
      via: "item.IsSplitButton = True assignment"
      pattern: "IsSplitButton = True"
---

<objective>
Add the `IsSplitButton As Boolean = False` property to `web/XjRibbonItem.xojo_code`.

Purpose: This single property is the only data-model change needed in the item class to enable SplitButton mode. It is read by the canvas Pressed event, DrawDropdownButton, DrawLargeButton, and LayoutTabs — all in wave 2. The property must exist before wave 2 plans compile.

Output: `web/XjRibbonItem.xojo_code` with `IsSplitButton As Boolean = False` inserted after the `IsToggleActive` property block.
</objective>

<execution_context>
@/Users/worajedt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/worajedt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/worajedt/Xojo Projects/XjRibbon/.planning/ROADMAP.md
@/Users/worajedt/Xojo Projects/XjRibbon/.planning/phases/07-web-phase-5/07-RESEARCH.md

<interfaces>
<!-- Current web/XjRibbonItem.xojo_code property list (lines 11–69). -->
<!-- IsSplitButton must be inserted after IsToggleActive (line 41) and before mMenuItems (line 43). -->

Existing property order (relevant excerpt):
  line 36-37: IsToggle As Boolean = False
  line 39-40: IsToggleActive As Boolean = False   ← INSERT AFTER THIS BLOCK
  line 43-45: mMenuItems() As WebMenuItem         ← before this
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IsSplitButton property to web XjRibbonItem</name>
  <files>web/XjRibbonItem.xojo_code</files>
  <action>
IMPORTANT: Quit Xojo IDE before editing this file (per project memory).

Open web/XjRibbonItem.xojo_code. The file is 72 lines. Locate the block ending at line 41:

```
	#tag Property, Flags = &h0
		IsToggleActive As Boolean = False
	#tag EndProperty
```

After that `#tag EndProperty` line (after line 41), insert exactly this block (blank line before, blank line after — matching the spacing pattern of all other properties in the file):

```
	#tag Property, Flags = &h0
		IsSplitButton As Boolean = False
	#tag EndProperty

```

The result: `IsSplitButton` sits between `IsToggleActive` and `mMenuItems() As WebMenuItem`.

Do NOT add `KeyTip As String` — web skipped Phase 4 keyboard nav.
Do NOT change any other property, method, or the class structure.
Preserve the exact tab indentation (one tab for `#tag Property`, two tabs for the property line).
  </action>
  <verify>
    <automated>grep -n "IsSplitButton" "/Users/worajedt/Xojo Projects/XjRibbon/web/XjRibbonItem.xojo_code"</automated>
  </verify>
  <done>grep output shows `IsSplitButton As Boolean = False` in web/XjRibbonItem.xojo_code. No other lines changed (file grows from 72 to 77 lines with the inserted block + blank lines).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Xojo IDE ↔ source file | IDE will overwrite manual edits if open during editing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | XjRibbonItem source file | accept | Low risk — property addition with no runtime security surface; file is compiled source, not user-facing data |
</threat_model>

<verification>
After this plan executes:
- `grep -c "IsSplitButton" web/XjRibbonItem.xojo_code` returns `1`
- File has `IsSplitButton As Boolean = False` between `IsToggleActive` and `mMenuItems`
- No other properties modified
</verification>

<success_criteria>
`web/XjRibbonItem.xojo_code` contains the `IsSplitButton As Boolean = False` property block, enabling wave 2 plans to compile references to `item.IsSplitButton`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-phase-5/07-01-SUMMARY.md`
</output>
