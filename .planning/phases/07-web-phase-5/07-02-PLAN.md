---
phase: "07-web-phase-5"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/XjRibbonGroup.xojo_code
autonomous: true
requirements:
  - REQ-705
must_haves:
  truths:
    - "XjRibbonGroup has AddSplitButton(caption, tag) returning XjRibbonItem with ItemType=2 and IsSplitButton=True"
    - "XjRibbonGroup has AddCheckBox(caption, tag, initialState) returning XjRibbonItem with ItemType=3, IsToggle=True, IsToggleActive=initialState"
    - "XjRibbonGroup has AddSeparator() adding an XjRibbonItem with ItemType=4"
  artifacts:
    - path: "web/XjRibbonGroup.xojo_code"
      provides: "Three factory methods for new control types"
      exports: ["AddSplitButton", "AddCheckBox", "AddSeparator"]
  key_links:
    - from: "web/MainWebPage.Shown"
      to: "XjRibbonGroup.AddSplitButton"
      via: "navGroup.AddSplitButton(caption, tag)"
      pattern: "AddSplitButton"
    - from: "web/MainWebPage.Shown"
      to: "XjRibbonGroup.AddCheckBox"
      via: "showHide.AddCheckBox(caption, tag)"
      pattern: "AddCheckBox"
---

<objective>
Add three factory methods to `web/XjRibbonGroup.xojo_code`: `AddSplitButton`, `AddCheckBox`, and `AddSeparator`.

Purpose: These methods are the public API surface through which application code creates the new Phase 5 control types. They construct `XjRibbonItem` instances with the correct property values and add them to the group's `mItems` array. No rendering or hit-test logic belongs here.

Output: `web/XjRibbonGroup.xojo_code` with three new method blocks inserted after `AddDropdownButton`, before the `#tag Property` section (currently line 54).
</objective>

<execution_context>
@/Users/worajedt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/worajedt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/worajedt/Xojo Projects/XjRibbon/.planning/ROADMAP.md
@/Users/worajedt/Xojo Projects/XjRibbon/.planning/phases/07-web-phase-5/07-RESEARCH.md

<interfaces>
<!-- Current web/XjRibbonGroup.xojo_code structure (79 lines). -->
<!-- The three new methods insert after AddDropdownButton's #tag EndMethod (line 52) -->
<!-- and before the first #tag Property line (line 54). -->

AddDropdownButton ends at line 52:
  #tag EndMethod
                        ← INSERT THREE NEW METHOD BLOCKS HERE
  #tag Property, Flags = &h0
      Caption As String
  #tag EndProperty

XjRibbonItem properties available after Plan 01 runs:
  item.Caption As String
  item.Tag As String
  item.ItemType As Integer
  item.IsToggle As Boolean
  item.IsToggleActive As Boolean
  item.IsSplitButton As Boolean    ← added by Plan 01

mItems is typed as: mItems() As XjRibbonItem
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AddSplitButton method</name>
  <files>web/XjRibbonGroup.xojo_code</files>
  <action>
IMPORTANT: Quit Xojo IDE before editing this file (per project memory).

Open web/XjRibbonGroup.xojo_code. After the `#tag EndMethod` that closes `AddDropdownButton` (currently line 52), insert this block (with a blank line before it):

```
	#tag Method, Flags = &h0
		Function AddSplitButton(caption As String, tag As String) As XjRibbonItem
		  Var item As New XjRibbonItem
		  item.Caption = caption
		  item.Tag = tag
		  item.ItemType = 2
		  item.IsSplitButton = True
		  mItems.Add(item)
		  Return item
		End Function
	#tag EndMethod

```

Use one tab for `#tag Method`/`#tag EndMethod`, two tabs for `Function`/`End Function` and the body lines — matching the indentation of all existing methods in the file.
  </action>
  <verify>
    <automated>grep -n "AddSplitButton" "/Users/worajedt/Xojo Projects/XjRibbon/web/XjRibbonGroup.xojo_code"</automated>
  </verify>
  <done>grep shows `AddSplitButton` defined as a Function in web/XjRibbonGroup.xojo_code.</done>
</task>

<task type="auto">
  <name>Task 2: Add AddCheckBox method</name>
  <files>web/XjRibbonGroup.xojo_code</files>
  <action>
After the `#tag EndMethod` that closes `AddSplitButton` (just inserted in Task 1), insert this block:

```
	#tag Method, Flags = &h0
		Function AddCheckBox(caption As String, tag As String, initialState As Boolean = False) As XjRibbonItem
		  Var item As New XjRibbonItem
		  item.Caption = caption
		  item.Tag = tag
		  item.ItemType = 3
		  item.IsToggle = True
		  item.IsToggleActive = initialState
		  mItems.Add(item)
		  Return item
		End Function
	#tag EndMethod

```

Key details:
- `initialState As Boolean = False` — has a default value, making it optional for callers
- `item.IsToggle = True` — enables the existing toggle-flip path in the Pressed event automatically (no extra Pressed branch needed for CheckBox)
- `item.ItemType = 3` — matches `kItemTypeCheckBox` constant (added in Plan 03)
  </action>
  <verify>
    <automated>grep -n "AddCheckBox" "/Users/worajedt/Xojo Projects/XjRibbon/web/XjRibbonGroup.xojo_code"</automated>
  </verify>
  <done>grep shows `AddCheckBox` with `initialState As Boolean = False` parameter in web/XjRibbonGroup.xojo_code.</done>
</task>

<task type="auto">
  <name>Task 3: Add AddSeparator method</name>
  <files>web/XjRibbonGroup.xojo_code</files>
  <action>
After the `#tag EndMethod` that closes `AddCheckBox` (just inserted in Task 2), insert this block:

```
	#tag Method, Flags = &h0
		Sub AddSeparator()
		  Var item As New XjRibbonItem
		  item.ItemType = 4
		  mItems.Add(item)
		End Sub
	#tag EndMethod

```

Key details:
- `Sub` not `Function` — no return value needed; caller does not need a reference to the separator
- No Caption or Tag assignment — separator is a layout-only zero-bounds item
- `item.ItemType = 4` — matches `kItemTypeSeparator` constant (added in Plan 03)
- The blank line after `#tag EndMethod` separates it from the start of the `#tag Property` section

After inserting all three methods, verify the final structure of the file: the `#tag Property` for `Caption As String` must still appear immediately after the last `#tag EndMethod`.
  </action>
  <verify>
    <automated>grep -n "AddSeparator\|AddCheckBox\|AddSplitButton\|#tag Property" "/Users/worajedt/Xojo Projects/XjRibbon/web/XjRibbonGroup.xojo_code"</automated>
  </verify>
  <done>
grep output shows all three methods defined (AddSplitButton, AddCheckBox, AddSeparator) and the `#tag Property` section follows the last `#tag EndMethod`. No existing methods (AddItem, AddLargeButton, AddSmallButton, AddDropdownButton) were modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Xojo IDE ↔ source file | IDE will overwrite manual edits if open during editing |
| Caller → AddCheckBox initialState | Boolean parameter; no injection surface in compiled Xojo code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | XjRibbonGroup source file | accept | Compiled source; no runtime security surface; factory methods only set item properties |
</threat_model>

<verification>
After this plan executes:
- `grep -c "AddSplitButton\|AddCheckBox\|AddSeparator" web/XjRibbonGroup.xojo_code` returns `3`
- `AddSplitButton` sets `item.ItemType = 2` and `item.IsSplitButton = True`
- `AddCheckBox` sets `item.ItemType = 3`, `item.IsToggle = True`, `item.IsToggleActive = initialState`
- `AddSeparator` sets `item.ItemType = 4` only
- All existing methods (AddItem, AddLargeButton x2, AddSmallButton, AddDropdownButton) unchanged
</verification>

<success_criteria>
`web/XjRibbonGroup.xojo_code` exposes `AddSplitButton`, `AddCheckBox`, and `AddSeparator`. The demo code in Plan 04 (MainWebPage) can call all three without compile errors.
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-phase-5/07-02-SUMMARY.md`
</output>
